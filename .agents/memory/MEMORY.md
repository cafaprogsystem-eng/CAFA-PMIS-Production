// hint: Logic changed on both sides. Requires understanding intent of each change.
- [Global Full Operational Access](global-full-operational-access.md) — PM+super_admin system-wide access; accessControl.ts helper; override audit trail (migration 020); self-review override requires overrideReason.
- [Revocable authenticated sessions](revocable-session-logout.md) — opaque server-backed sessions are the sole HTTP/realtime authority; legacy numeric cookies never authenticate.
- [Connectivity evidence model](connectivity-evidence-model.md) — only bounded CAFA health-probe transport failures verify Offline; preserve auth/access outcomes across public probe success.
- [Budgets Overview data integrity rules](budgets-overview-data.md) — SPO uses userScope (not buildScope); null≠zero for spent; budgetByCurrency always returned; currency-aware formatCurrency/formatPercent; donor beneficiaries/spend from real queries.
- [Planning Workspace Architecture](planning-workspace-arch.md) — Plans page is canonical (/planning + /planning-dashboard both redirect); two data sources; API extension for awaitingApproval; no budget/risks KPIs in workspace.
- [Project deletion architecture](project-deletion-arch.md) — soft vs. permanent mode; pure helper; schema; cascade order; perms (ED+PM); audit_log survives permanent delete.
- [UI Design System State](ui-design-system.md) — Final polish done; font-medium everywhere (no font-semibold/bold in card titles/labels); Budget order: Remaining→Allocated→Spent; StatPill bg-card border-border/60; chart tooltip has labelStyle/itemStyle; sidebar all bg-card/border-border.
- [Sector taxonomy — single source of truth](sector-taxonomy.md) — 7 canonical main sectors (post-unification); MAIN_SECTORS+SECTORS alias+SUB_SECTORS+ASSISTANCE_MODALITIES; PR_SECTORS = General/Cross-Cutting + MAIN_SECTORS; never add local sector lists to pages/components.
- [Comments/revision gate duplication](comments-system.md) — role→type allow-list lives in two places (server + client comments-panel); must change in lockstep.
- [PG date columns return JS Date objects](pg-date-types.md) — `::date` cast in a pg query returns a JS Date, not a string; always normalise with `val instanceof Date ? val.toISOString().slice(0,10) : String(val).slice(0,10)` before string ops.
- [Email system configuration](email-system.md) — mailer supports EMAIL_PROVIDER=resend|sendgrid|smtp|stub; stub mode active when EMAIL_ENABLED≠true; all sends logged to email_logs table; nodemailer installed for SMTP. **Critical:** flipping EMAIL_ENABLED=true with EMAIL_PROVIDER≠resend crashes the whole server at boot (monthly-reporting-reminder idempotency guard) — must use resend, or set MONTHLY_REPORTING_ENABLED=false, before enabling real delivery in production.
- [Notification delivery contract](notification-delivery-contract.md) — in-app/email decisions are independent; mandatory overrides remain; digest scheduling is unavailable until a scheduler exists.
- [Notification email_verified gate](notification-email-verified-gate.md) — optional emails withheld when email_verified=false; mandatory emails bypass; null/undefined treated as verified (legacy compat); frontend switches must be HTML-disabled not pointer-events-none.
- [Notification recipient integrity](notification-recipient-dedupe.md) — only active users receive notifications; source-event keys are atomically claimed per recipient before any channel side effect.
- [Public routes must wrap outside AuthGate](auth-gate-public-routes.md) — any unauthenticated page (e.g. invite acceptance) needs a wouter Switch above AuthGate, plus a matching PUBLIC_PATHS entry in `attachCurrentUser`.
- [Wouter shared new/edit route](wouter-shared-new-edit-route.md) — for a detail page that handles both create and edit, register only `/resource/:id` and treat `id === "new"` as the create sentinel; a sibling `/resource/new` route silently breaks create mode.
- [Wouter query-string redirects](wouter-location-query-string.md) — use `window.location.search` for safe redirect context; Wouter's useLocation value is pathname-only here.
- [Object storage provider contract](object-storage-providers.md) — AWS S3 is production-standard; alternate providers remain explicit/conditional and must not mask incomplete selected-provider configuration.
- [Orval codegen duplicate-export conflict](orval-codegen-conflicts.md) — use `$ref: "…/XyzInput"` (never `XyzBody`) + remove `schemas` from zod orval config to prevent TS2308.
- [Dockerfile runner script COPY list](dockerfile-runner-script-copy-list.md) — the production runner stage copies `scripts/*.mjs` by explicit filename, not by directory; a new one-off script must be added to that `COPY` line or it fails at container runtime with `MODULE_NOT_FOUND` (already recurred twice: `capture-training-screenshots.mjs`, `create-first-admin.mjs`).
- [CurrentUser shape from useGetMe](current-user-shape.md) — `useGetMe()` returns `CurrentUser = { user: User, permissions: string[] }`; access role via `me?.user.role`, NOT `me?.role` (causes TS2339).
- [AI lib composite build](ai-lib-composite-build.md) — `lib/integrations-openai-ai-server` is composite; must appear in root `tsconfig.json` references AND `artifacts/api-server/tsconfig.json` references; image client uses `response.data?.[0]` optional chain (OpenAI SDK v6 types `data` as possibly undefined).
- [User type languagePreference cast](user-type-extra-fields.md) — generated `User` type from api-client-react doesn't index-sign; cast via `(user as unknown as Record<string, string|undefined>)?.fieldName` to access extra fields like `languagePreference` without TS2339.
- [Offline PWA blocked-action pattern](offline-pwa-blocked-actions.md) — DELETE + user mgmt + final_approve transitions + budget + AI settings are BLOCKED offline (OfflineBlockedError), not queued; safe mutations (report submit, risk create, etc.) are queued (OfflineQueuedError). Never use `navigator.onLine` in sync logic — use the probed `isOnline` state from SyncContext.
- [Offline draft replay ordering](offline-draft-replay-ordering.md) — persist a snapshot linked to its stable operation ID before inserting a replay-eligible queue item; settle only the matching snapshot.
- [Recent items architecture](recent-items-arch.md) — user-scoped localStorage (`cafa:recent:${userId}`), pure utils in lib/recent-items.ts, hook in hooks/use-recent-items.ts; hooks must be declared before any effect that references them (TDZ); ICON_MAP is JSX defined at module level in command-palette.tsx.
- [Favorites architecture](favorites-arch.md) — user-scoped localStorage (`cafa:favorites:${userId}`); pure utils in lib/favorites.ts; hook in hooks/use-favorites.ts; cleared on logout alongside recent items; rankScore(path, favorites, recentMap) used in command-palette groups useMemo + global-search sortedProjects/sortedPlans memos.
- [Communication attachment capability](communication-upload-capability.md) — message attachments/voice use a dedicated capability and parent-authorised proxy URLs, never document-upload authority or raw storage links.
- [Express middleware breaks req.params typing](express-middleware-params-cast.md) — adding `requirePerm()` middleware to a route with `:id` param changes overload resolution; `req.params.id` becomes `string | string[]`; fix with `req.params.id as string` cast. Also: `CREATE INDEX CONCURRENTLY` cannot run inside a psql transaction block — must use individual `-c` calls, not semicolon-separated blocks.
- [Dashboard analytics data integrity](dashboard-analytics-bugs.md) — several recurring pitfalls found in routes/dashboard.ts; see topic file.
- [Donor Portfolio table architecture](donor-portfolio-arch.md) — bar chart replaced with analytical table; server groups in TS (not SQL) via single projects+donors JOIN; SortableCol must include donor sort keys; DonorPortfolioEntry extended with canonical fields (backward compat kept).
- [Project final_approve document gate](project-doc-gate.md) — final_approve requires category='agreement' AND category='budget' docs in project_documents; assertStateAllowed in projects.ts is async (takes projectId, queries project_states); in plans.ts it's sync (takes stateId directly).
- [Report creation schema](report-creation-schema.md) — CreateReportBody requires `kind` (string e.g. "monthly") and `period` (string e.g. "2026-06"); `type` is wrong field name; request_revision on reports needs reports.approve.coordination (senior_coordinator), not reports.approve.final (PM).
- [State role RBAC security rules](state-role-rbac.md) — state_manager and state_officer must NOT have unrestricted audit.view; /audit-log route enforces state/sector scoping server-side via JOIN subqueries (no schema change needed).
- [Audit workspace safety contract](audit-workspace-safety.md) — Audit filters use the generated schema plus strict refinements; audit change values are allowlisted, never generically rendered.
- [Login response user shape](login-response-shape.md) — POST /auth/login returns `{user, permissions}`; user object must include `status` field (not in CurrentUser type — added as explicit spread in res.json); login always implies active but frontend may need status explicitly.
- [Conversations module patterns](conversations-patterns.md) — key API behaviors: POST /conversations direct returns 200 for existing DM (dedup), 201 for new; GET /notifications returns {items,unread} NOT {notifications}; GET /audit-log returns flat list NOT {rows}; pin limit=10 per conv; ANNOUNCEMENT_ROLES excludes senior_coordinator (PM and above only); deleted messages must CASE WHEN blank body+attachments in SELECT.
- [i18n Phase 1 Architecture](i18n-phase1-arch.md) — Language infrastructure unlocked; LanguageProvider exposes lang/direction/setLang; RadixDirectionBridge wraps app; 17 ar/ stubs created; flash prevention in index.html; LOGIN_STRINGS deleted; Phase 2+ does module translation + logical CSS.
- [i18n Phase 2 — Arabic Glossary & Standards](i18n-phase2-glossary.md) — Authoritative glossary in CAFA_ARABIC_GLOSSARY.md; common/nav/errors seeded; 4 EN conflicts documented; 13 label maps queued for Phase 3 migration; 63 consistency tests added.
- [i18n Phase 3 — Shell & RTL Foundation](i18n-phase3-shell.md) — All shell components RTL-wired; 93 shell tests in i18n-shell.test.ts; glossary alignment rule for ar/common.json rewrite; Phase 4 = business modules.
- [RTL business-surface guardrails](rtl-business-guardrails.md) — Audit every business form/view, not representative pages only; permit physical sides solely for paired overlay/centering geometry.
- [Project registration form tab structure](project-reg-form-tabs.md) — 7-tab layout: Basic/Location/Donor/Timeline/Team/Documents/Review; TABS+TAB_FIELDS+TabId at module level; handleFormSubmit routes to first tab with errors; startDate+endDate in Timeline tab only (not Basic).
- [Hierarchical performance architecture](hierarchical-performance-arch.md) — indicator→project→sector hierarchy; `computeHierarchicalPerformance` in performanceEngine.ts; GET /dashboard/hierarchical-performance; useHierarchicalPerformance hook; sectors+PerformanceScore imports retained for Overview tab.
- [Dashboard legacy scoring model removed](dashboard-legacy-scoring.md) — composite score (6 weighted dims, Excellent/Good/Needs Follow-Up/Critical tiers) fully removed from all Dashboard tabs; activityCompletionPct/reportingCompliancePct now return null for zero-denominator cases.
- [Dashboard hook/component stability architecture](dashboard-hook-arch.md) — all useMemo/useState before any early return; no inner components used as JSX (module scope only); ESLint rule + 35-test suite guard against recurrence.
- [API client declaration drift](api-client-react-dist-rebuild.md) — consumer typechecks validate emitted declarations, so verify generated client artifacts before changing consumers.
- [Plan Details view/edit architecture](plan-detail-view-edit.md) — isEditing=useState(isNew); existing plans default view; Cancel resets form+exits; Save success sets isEditing(false); Delete in overflow DropdownMenu; formatPlanType unified with Plans table.
- [PG date in date inputs](pg-date-input-fix.md) — API serialises PG date columns as full ISO strings (e.g. "2026-07-01T00:00:00.000Z"); date inputs need "YYYY-MM-DD"; always slice(0,10) before populating form state.
- [Plan approval lock + reopen architecture](plan-approval-lock.md) — isPlanCurrentlyEditable() checks approvals table (not just status); canEdit uses plans.update; plans.reopen now granted to SPC+TC; TC sector-scoped at endpoint.
- [Create Plan modal architecture](create-plan-modal-arch.md) — modal replaces /plans/new; 7 core fields only; plans.create gates visibility (not projects.create); ?edit=1 param triggers edit mode; canCreate fixed in plans.tsx.
- [Tab 4 Activities architecture](activity-tab-arch.md) — isActivityComplete pure fn; locality scoping via Select (not free-text); state inherited read-only; reconciliation on locality remove + state change; finish gate; 35 tests.
- [Sector Budget currency architecture](sector-budget-currency-arch.md) — budgetByCurrency[] per sector; null≠zero for financial fields; exception counts (overallocated/overspent) use COALESCE(0); utilisationPct is float not rounded int; >100% preserved.
- [Reports Architecture](reports-architecture.md) — 4 canonical types/frequencies; full vs short workflow chain; new KPI model; migration 005 deduplication; submitted_at reset on re-submit; SELECT FOR UPDATE in transitions.
- [Reports author-based workflow](reports-author-workflow.md) — workflow_path (state_authored/technical_authored) set at CREATE, immutable; SOM is view-only; state_reviewed removed from all status arrays; orphaned submitted_by_id must use JOIN guard in migrations.
- [Project Report Form Architecture](project-report-form-arch.md) — currency from project cast; null≠zero planned; split validateDraft/validateSubmit/buildPayloadData; true ARIA tabs (hidden class); sticky footer outside scroll; dirty-form AlertDialog; unplanned activities excluded from linked totals.
- [Activity Reports Architecture](activity-reports-arch.md) — /reports/activity route live; TC scope uses p.sector ONLY (not r.sector); activityTitle+activityCode in reportSelect JOIN; activity-facet endpoint; author gating = same VALID set as project reports.
- [Activity Reports Security Architecture](activity-reports-security.md) — 6 invariants: TC scope via p.sector only (both getReportSector + mixed-query stats); CREATE validation chain order; identity immutability in PATCH; uniqueness key = activity+state+period (Migration 010); effectiveSector = p.sector; duplicate-check endpoint for activity type.
- [Activity Report Link Modes](activity-report-link-modes.md) — standalone/activity/project three-mode UI; activityName required in all modes; activityId no longer mandatory; migration 012 adds activity_name TEXT; duplicate-check skipped for null activityId.
- [HQ Location Architecture](hq-location-arch.md) — explicit location_type column; COALESCE inference; formatLocation/LocationSelector as sole branching points; HQ_SENTINEL; plan code CAFA-PLAN-HQ-NNN.
- [HQ Operational Location flag](hq-ops-flag.md) — projects.has_hq_operations (migration 016, DEFAULT false); PMR gate uses this flag NOT management_level; test mocks for project SELECT must include hasHqOperations field.
- [Dashboard analytics authz](dashboard-analytics-authz.md) — new analytics endpoints need requirePerm middleware + state-role clamping on every location/report query; fail closed when stateId is null.
- [Rendered page tests](rendered-page-tests.md) — full ReportsPage can be rendered in vitest; mock api-client hooks with module-level stable objects (fresh objects per render cause infinite effect loops) + TooltipProvider + jsdom shims.
- [jsdom localStorage unavailable under Node 26](jsdom-localstorage-node26.md) — UNRESOLVED: 16 frontend test files (159 tests) fail with "Cannot read properties of undefined (reading 'getItem')" on `window.localStorage`; needs `--localstorage-file` (or equivalent) passed to the vitest/Node process; confirmed pre-existing on main via a stash-compare, not caused by any in-progress feature work.
- [Project scheduled reporting frequency](project-reporting-frequency.md) — nullable, no default/backfill, on_demand excluded; PATCH presence-flag pattern; soft client warning only; null displays "Not Configured".
- [SPO project-assignment scope](spo-project-assignment-scope.md) — SPO scope = state clamp + project_assignments membership on every read path; null-state roles fail closed; deep links resolve via authorised single-item endpoints.
- [PMR notification routing](pmr-notification-routing.md) — notifyNextApprover is workflow-path-aware (technical_authored submit → SPC, never TC); actorsForEntity('report') must include author_id.
- [SPR identity immutability](spr-identity-immutability.md) — program_state identity fields 409 on PATCH (present-key); SPO/SOM with null stateId fail-closed on SPR create.
- [HQ Sector Report author gate](hqsr-author-gate.md) — TC (exact sector) + super_admin + SPC fallback (vacancy-checked, ENABLED; PM reviews SPC-authored only); canonical sector required for all authors.
- [Report view auth helper](report-view-auth.md) — assertCanViewReport must fail closed for SPO/SOM with stateId=null; route tests mock it, so keep real-logic unit tests in lib/reportAuth.test.ts.
- [Submit-gate validation pitfalls](submit-gate-validation-pitfalls.md) — strict YYYY-MM-DD round-trip parsing (Date.parse accepts Feb 30) + fail-closed on-demand detection via kind OR sections.frequency.
- [Report duplicate-check queries](report-duplicate-checks.md) — guards mirroring reports unique indexes need status exclusion AND migration_is_duplicate=FALSE; read the LAST index recreation in run-migrations.ts, not the first.
- [HQSR location integrity](hqsr-location-integrity.md) — hq_sector rows must be state_id/project_id NULL; 422 + forced-NULL INSERT + DB CHECK; migration fails loudly on unremediated Class B rows.
- [SPR author gate](spr-author-gate.md) — program_state authored by SPO / vacancy-verified SOM fallback / super_admin only; fallback roles get a narrow type permission bounded to own-report + own-state across the whole lifecycle.
- [Radix Select spurious empty onValueChange](radix-select-empty-onvaluechange.md) — programmatic reset to an unmounted item fires onValueChange(""); guard handlers with `if (val)` when hydrating edit forms.
- [Report analytics join rule](spr-analytics-joins.md) — sector-scoped report analytics must LEFT JOIN projects + COALESCE(r.sector, p.sector); INNER JOIN drops project_id=NULL reports.
- [SPR comment section taxonomy](spr-comment-sections.md) — canonical section keys or null only for program_state comments (422 otherwise); returned-draft authors read feedback via a narrow author-gated read-only path, never widened comment perms.
- [Plan effective sectors](plan-effective-sectors.md) — strict precedence chain (sectors JSONB → [sector] → [project sector]); never OR-fallback predicates; sector-changing PATCH re-guarded in-transaction.
- [Reports zero-residual closure](reports-zero-residual.md) — REP-ZR sentinel suite guards Reports; JSONB validators must type-narrow entries (null entry → 500); no startup DDL in route files.
- [Risk Register semantics](risk-register-audit.md) — 3×3 scoring, severity mirrors impact, app-level enum validation, no delete/get-by-id routes, plan links owned by plans.ts, KPI must match riskLevel=critical list.
- [Budgets module audit invariants](budgets-module-audit.md) — no state-level expenditure source (show — never a share); allocation UNIQUE/FK/CHECK in migration 027; deleted_at filters on all budget analytics.
- [Canonical attachment parent access](drive-files-parent-access.md) — attachment authority comes from the parent; historical imports derive their target from locked source evidence, never caller metadata.
- [Plan comment read exception](plan-comment-read-exception.md) — SPO/SOM narrowly read revision_request comments on returned draft plans; all other roles have comments.create.
- [Allocation cap enforcement](allocation-cap-enforcement.md) — SUM(allocations) ≤ budget_total enforced in-transaction under FOR UPDATE row lock; 422 over_allocation; migration 029 warns on residuals.
- [Budget historical over-allocation register](budget-historical-overallocation.md) — 8 projects await human finance decisions; no auto-remediation allowed; BUD-HIST sentinels pin the outcome.
- [Budget dashboard access contract](budget-endpoint-gates.md) — mixed dashboards preserve scoped operations while redacting finance; dedicated financial analytics stay restricted.
- [Notification taxonomy contract](notification-taxonomy-contract.md) — current notification events use one registry; map historical aliases at presentation, never by rewriting history.
- [Communication lifecycle integrity](communication-data-lifecycle.md) — protect future messaging writes with key tables/locks and NOT VALID FKs when legacy data cannot be safely auto-repaired.
- [Communication realtime authority](communication-realtime-authority.md) — room fan-out rechecks canonical access; private hides use actor-only invalidation and operational viewers never become members.
- [Communication read state and pagination](communication-read-state-pagination.md) — read truth is membership-level only; operational non-members have null unread state; list cursors use activity then ID.
- [File-picker button activation](file-picker-button-activation.md) — visible Button controls must explicitly trigger hidden file inputs; never rely on a nested label around an interactive button.
- [Concurrent UI rebase scope](concurrent-ui-rebase-scope.md) — rebase UI work onto newly merged shared-component changes before review; retain main behaviour and reapply only the feature’s local edits.
- [Shared Project and Plan record viewer](shared-record-detail-viewer.md) — read-only record links open through the coordinator; query-bearing edit URLs and all in-dialog links stay canonical.
- [Express rate-limit IPv6 keys](express-rate-limit-ipv6.md) — custom IP fallback keys must use the library helper to remain IPv6-safe.
- [IndexedDB primary-key bridge](indexeddb-primary-key-bridge.md) — use a new canonical database and atomic, ownership-validated copy when an IndexedDB primary key must change.

- [Global Location Context Architecture](location-context-arch.md) — HQ roles get header selector (LocationProvider + GlobalLocationSelector); state-scoped roles see nothing; pages sync via useEffect on selectedStateId change.
- [Location Context stateId filtering contract](location-context-stateId-contract.md) — financial dashboard endpoints need applyFilterParams for HQ narrowing; generated hooks with no params need custom useQuery+customFetch(url+param); beneficiaries uses resolveLocationContext.
- [Effective Access Inspector parity](effective-access-inspector-parity.md) — Inspector must mirror every canonical capability and distinguish direct project assignments from normal State scope.
- [Offline draft scope recovery](offline-draft-scope-recovery.md) — Restore drafts only after current authorised references load; clear every stale top-level and nested relationship.
- [Production PWA browser certification](production-pwa-browser-certification.md) — A Vite dev worker cannot prove offline app-shell reload; require a routed production build and fail closed on missing certification inputs.
- [Canonical attachment promotion consistency](canonical-attachment-promotion.md) — External object promotion needs a persisted final identity and one parent→attachment lock order so retries and parent deletion stay safe.
- [Report attachment registration resilience](report-attachment-registration.md) — Persist safe attachment IDs and type at registration; clear each local file only after its own success so report retries cannot duplicate evidence.
- [Attachment upload cleanup durability](attachment-upload-cleanup.md) — Retry state must live in a parent-independent outbox enqueued before source deletion, with leased provider cleanup.
- [Socket.IO credentialed origins](socketio-credentialed-origins.md) — Socket.IO CORS covers polling; the same policy needs `allowRequest` to gate WebSocket upgrades.
- [Responsive sidebar rail](responsive-sidebar-rail.md) — Persist desktop collapse only at desktop width; rail tooltips must use logical inward placement and be browser-verified in RTL.
- [Tracked migration immutability](tracked-migration-immutability.md) — Never alter a migration after its checksum can be recorded; ship corrective data changes as a new forward-only migration.
- [Sidebar visual regression stability](sidebar-visual-regression.md) — Use live rendered dimensions for responsive snapshot clips; reject every snapshot-update flag form.
- [Dynamic i18n resource contracts](dynamic-i18n-resource-contracts.md) — Dynamic UI translation families need EN/AR resources, source-resolution tests, and safe localized enum fallbacks.
- [Async presence transition ordering](async-presence-transition-ordering.md) — asynchronous persistence and recipient reauthorisation must version-fence presence events after every await.
- [Realtime operational authority](realtime-operational-authority.md) — rooms and hints optimise delivery only; each record watch, lock, and event must reuse canonical access.
